import { AppBar } from '@mui/material';

const Footer = () => {
  return (
    <AppBar component='footer' position='static' elevation={0} color='transparent' className='relative z-[1]'></AppBar>
  );
};

export default Footer;
